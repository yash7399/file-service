$sw = [System.Diagnostics.Stopwatch]::StartNew()
$buffer = New-Object byte[] (1MB)
$out = [System.IO.File]::OpenWrite("testfile.bin")

for ($i=0; $i -lt 1000; $i++) {
    $out.Write($buffer, 0, $buffer.Length)
}

$out.Close()
$sw.Stop()
$sw.Elapsed.TotalSeconds